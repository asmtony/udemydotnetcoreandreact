# Ctrl + j to toggle the terminal panel 
# create the react Application with create react app
npx create-react-app client-app --use-npm --typescript

#remember that if you ever clone the project you will need to run npm install
# react template snippet
rafce

# start 
npm start

#Install axios 
npm install axios

# Install semantic-ui
npm install semantic-ui-react
# also add the cdn semantic to public=>index.html 

npm install uuid

npm install @types/uuid

# Install mobx lite
npm install mobx mobx-react-lite

#Install react router
npm install react-router-dom
#Install the types
npm install @types/react-router-dom

# Install toast
npm install react-toastify

# Install react final form
npm install react-final-form final-form (also installs revalidate package)

#Section 11 (may need to watch again due to typings file)
# Install react-widgets to get the date picker (then follow the warning as per below)
npm install react-widgets react-widgets-date-fns
# npm WARN react-widgets-date-fns@4.1.0 requires a peer of date-fns@^2.0.0 but none is installed. You must install peer dependencies yourself.
npm install date-fns@^2.0.0

# Install revalidate for validating forms
npm install revalidate
# Also types file for revalidate
npm install @types/revalidate 
